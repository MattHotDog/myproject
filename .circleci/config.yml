version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:2022.05
    steps:
      - command: make test
  build:
    docker:
      - image: cimg/base:2022.05
    steps:
      - run: echo "hello world"

workflows:
  build1AndBuild2:
    jobs:
      - test
      - build:
          requires:
            - test
